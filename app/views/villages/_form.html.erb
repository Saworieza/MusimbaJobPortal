<%= form_for(village) do |f| %>
  <% if village.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(village.errors.count, "error") %> prohibited this village from being saved:</h2>

      <ul>
      <% village.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :sublocation_id %>
    <%= f.text_field :sublocation_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
