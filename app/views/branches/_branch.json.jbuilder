json.extract! branch, :id, :branch_name, :contact_person, :role, :box_number, :size, :phone, :email, :building, :street, :company_id, :created_at, :updated_at
json.url branch_url(branch, format: :json)