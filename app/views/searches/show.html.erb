<section class="wrapper">
  <div class="container">
    <div class="row">
    	<div class="col-md-9">
    		<h3>Search Results</h3>
    		<!-- to be fixed better -->
  			<h4><%= @job.count %> Jobs Found<br></h4>

				<%# if @search.present? %>
					<%= render @search.jobs %>
				<%# else %>
					<!-- <h1>No Results Found</h1> -->
				<%# end %>
			</div>

			<div class="col-md-3">
        <%= render 'jobs/popularity' %>
      </div>
		</div>
	</div>
</section>



